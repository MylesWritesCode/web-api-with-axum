[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
sea-orm = "0.8.0"
entity = { path = "../entity" }
